# Stage: Dev (không cần nginx)
FROM node:22-alpine

WORKDIR /app

# Copy package.json và package-lock.json trước để cache dependency
COPY package*.json ./

# Cài dependencies (bao gồm devDependencies) + tránh lỗi peer deps
RUN npm install --legacy-peer-deps

# Copy toàn bộ source code
COPY . .

# Expose port Vite dev server
EXPOSE 5173

# Chạy Vite server với host 0.0.0.0 để container có thể truy cập
CMD ["npm", "run", "dev", "--", "--host"]
